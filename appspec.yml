version: 0.0
os: linux

files:
  - source: /
    destination: /home/ec2-user/app
    overwrite: true

hooks:
  AfterInstall:
    - location: scripts/after_install.sh
      timeout: 300
      runas: root


